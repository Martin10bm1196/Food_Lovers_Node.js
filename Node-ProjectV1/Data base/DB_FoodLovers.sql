/*create database DB_Restaurantes;

  use DB_Restaurantes;


    create table usuario(
      cedula int unsigned primary key,
      nombreCompleto varchar (150),
      email varchar (50),
      contrasenna varchar(50),
      telefono varchar (50), 
      provincia varchar(50),
      roll varchar(15)
    );
  show tables;

  describe usuario;*/